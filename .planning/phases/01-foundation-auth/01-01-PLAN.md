---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - .env.local
  - .env.example
  - components.json
  - src/app/layout.tsx
  - src/app/globals.css
  - supabase/config.toml
  - supabase/migrations/20260101000000_create_schema.sql
  - supabase/migrations/20260101000001_create_profiles.sql
  - supabase/migrations/20260101000002_create_companies.sql
  - supabase/migrations/20260101000003_create_projects.sql
  - supabase/migrations/20260101000004_create_materials_inventory.sql
  - supabase/migrations/20260101000005_create_reservations_transfers.sql
  - supabase/migrations/20260101000006_create_suppliers_procurement.sql
  - supabase/migrations/20260101000007_create_employees_attendance.sql
  - supabase/migrations/20260101000008_create_cash_advances_loans.sql
  - supabase/migrations/20260101000009_create_payroll.sql
  - supabase/migrations/20260101000010_create_project_costs.sql
  - supabase/migrations/20260101000011_create_expenses.sql
  - supabase/migrations/20260101000012_create_equipment.sql
  - supabase/migrations/20260101000013_create_documents.sql
  - supabase/migrations/20260101000014_create_notifications_audit.sql
  - supabase/migrations/20260101000015_create_subscriptions.sql
  - supabase/migrations/20260101000016_create_rls_policies.sql
  - supabase/migrations/20260101000017_create_transaction_functions.sql
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - AUTH-10

must_haves:
  truths:
    - "Next.js project builds and starts on localhost"
    - "Supabase local instance starts with construction schema"
    - "All 25+ tables exist in construction schema with correct columns"
    - "RLS is enabled on every table with company_members-based isolation policies"
    - "All 6 PostgreSQL transaction functions are deployed and callable"
    - "updated_at triggers fire on all relevant tables"
    - "Composite index on company_members(user_id, company_id) exists"
    - "Partial unique index enforces one owner per company"
  artifacts:
    - path: "supabase/migrations/20260101000000_create_schema.sql"
      provides: "construction schema with grants"
      contains: "CREATE SCHEMA IF NOT EXISTS construction"
    - path: "supabase/migrations/20260101000016_create_rls_policies.sql"
      provides: "RLS policies for all tables"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "supabase/migrations/20260101000017_create_transaction_functions.sql"
      provides: "6 atomic inventory functions"
      contains: "fn_record_delivery"
    - path: "package.json"
      provides: "Next.js project dependencies"
      contains: "next"
  key_links:
    - from: "supabase/migrations/20260101000016_create_rls_policies.sql"
      to: "supabase/migrations/20260101000002_create_companies.sql"
      via: "RLS policies reference company_members table"
      pattern: "auth.user_company_id"
---

<objective>
Bootstrap the entire project: create the Next.js application, initialize Supabase, install all Phase 1 dependencies, and deploy the complete database schema (all 25+ tables, RLS policies, triggers, helper functions, and all 6 transaction functions) via Supabase migrations.

Purpose: Every subsequent plan depends on the project existing and the database schema being in place. This is the foundation for auth, the app shell, and all future phases.
Output: A buildable Next.js project with a fully migrated Supabase local database containing the complete construction schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@MRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project, install dependencies, configure Supabase</name>
  <files>
    package.json, tsconfig.json, next.config.ts, .env.local, .env.example,
    components.json, src/app/layout.tsx, src/app/globals.css, src/app/page.tsx,
    supabase/config.toml
  </files>
  <read_first>
    .planning/research/STACK.md
    MRD.md (section 4: Folder Structure, section 6: UI/UX Plan for design tokens)
  </read_first>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack` in the project root. If the project root is not empty (has .planning, MRD.md, etc.), create in a temp dir and move files, OR use `--yes` flags to handle existing files.

    2. Install Phase 1 dependencies (exact versions from STACK.md):
       ```
       npm install @supabase/supabase-js@2.100.0 @supabase/ssr@0.9.0
       npm install @reduxjs/toolkit@2.11.2 react-redux@9.2.0
       npm install react-hook-form@7.72.0 @hookform/resolvers@5.2.2 zod@4.3.6
       npm install date-fns@4.1.0 sonner@2.0.7 nuqs@2.8.9
       npm install -D vitest@4.1.1
       ```
       Do NOT install libraries not needed until later phases (recharts, stripe, jspdf, papaparse, react-dropzone, @tanstack/react-table, @tanstack/react-virtual, resend, playwright).

    3. Initialize shadcn/ui:
       ```
       npx shadcn@latest init
       ```
       Select: New York style, Slate base color, CSS variables = yes.
       Then install base components needed for Phase 1:
       ```
       npx shadcn@latest add button input card form label toast separator avatar dropdown-menu sheet badge
       ```

    4. Configure design tokens in `src/app/globals.css` (per D-15):
       - Dark Slate-900 sidebar background
       - White content area
       - Blue-600 as primary (hsl(220, 83%, 53%))
       - Amber-500 as accent (hsl(38, 92%, 50%))
       - Inter font via next/font
       - 0.5rem border radius (set --radius: 0.5rem in CSS variables)

    5. Create `.env.local` with placeholder values:
       ```
       NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
       NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-anon-key>
       SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>
       NEXT_PUBLIC_APP_URL=http://localhost:3000
       ```

    6. Create `.env.example` with the same keys but no values (just comments explaining each).

    7. Initialize Supabase:
       ```
       npx supabase init
       ```
       Then update `supabase/config.toml` to add `construction` to the exposed schemas:
       ```toml
       [api]
       extra_search_path = ["public", "construction"]
       schemas = ["public", "construction"]
       ```

    8. Update `next.config.ts` — no special config needed for Phase 1, keep defaults.

    9. Update `src/app/layout.tsx` to use Inter font via `next/font/google`, set metadata title to "Construction Management System", and add basic html/body structure.

    10. Update `src/app/page.tsx` to be a simple redirect to `/dashboard` (or temporary "Hello" page since auth isn't set up yet).
  </action>
  <verify>
    <automated>cd /Users/berltreasurecampomanes/Documents/Github\ Builds/construction && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "@supabase/supabase-js": "2.100.0"
    - package.json contains "@supabase/ssr": "0.9.0"
    - package.json contains "@reduxjs/toolkit": "2.11.2"
    - package.json contains "react-hook-form": "7.72.0"
    - package.json contains "zod": "4.3.6"
    - .env.example contains NEXT_PUBLIC_SUPABASE_URL
    - .env.example contains NEXT_PUBLIC_SUPABASE_ANON_KEY
    - .env.example contains SUPABASE_SERVICE_ROLE_KEY
    - supabase/config.toml contains "construction"
    - src/app/globals.css contains "--radius: 0.5rem"
    - src/app/layout.tsx contains "Inter"
    - components.json exists
    - npm run build exits 0
  </acceptance_criteria>
  <done>Next.js project builds successfully with all Phase 1 dependencies installed, shadcn/ui initialized, design tokens applied, and Supabase CLI initialized with construction schema exposure configured.</done>
</task>

<task type="auto">
  <name>Task 2: Create all 18 Supabase migration files (complete database schema)</name>
  <files>
    supabase/migrations/20260101000000_create_schema.sql,
    supabase/migrations/20260101000001_create_profiles.sql,
    supabase/migrations/20260101000002_create_companies.sql,
    supabase/migrations/20260101000003_create_projects.sql,
    supabase/migrations/20260101000004_create_materials_inventory.sql,
    supabase/migrations/20260101000005_create_reservations_transfers.sql,
    supabase/migrations/20260101000006_create_suppliers_procurement.sql,
    supabase/migrations/20260101000007_create_employees_attendance.sql,
    supabase/migrations/20260101000008_create_cash_advances_loans.sql,
    supabase/migrations/20260101000009_create_payroll.sql,
    supabase/migrations/20260101000010_create_project_costs.sql,
    supabase/migrations/20260101000011_create_expenses.sql,
    supabase/migrations/20260101000012_create_equipment.sql,
    supabase/migrations/20260101000013_create_documents.sql,
    supabase/migrations/20260101000014_create_notifications_audit.sql,
    supabase/migrations/20260101000015_create_subscriptions.sql,
    supabase/migrations/20260101000016_create_rls_policies.sql,
    supabase/migrations/20260101000017_create_transaction_functions.sql
  </files>
  <read_first>
    MRD.md (section 3: Database Schema — ALL table definitions, section 8: Supabase RLS Strategy, section on Transaction Functions)
    .planning/research/PITFALLS.md (Pitfall 1: RLS on custom schema, Pitfall 2: N+1 policy evaluations)
  </read_first>
  <action>
    Create all 18 migration files using the EXACT SQL from MRD.md. Copy the SQL verbatim — do not paraphrase or modify column names, types, constraints, or indexes. The MRD.md is the single source of truth for the schema.

    **Migration 000 — create_schema.sql:**
    ```sql
    CREATE SCHEMA IF NOT EXISTS construction;
    GRANT USAGE ON SCHEMA construction TO anon, authenticated, service_role;
    GRANT ALL ON ALL TABLES IN SCHEMA construction TO anon, authenticated, service_role;
    GRANT ALL ON ALL SEQUENCES IN SCHEMA construction TO anon, authenticated, service_role;
    GRANT ALL ON ALL ROUTINES IN SCHEMA construction TO anon, authenticated, service_role;
    ALTER DEFAULT PRIVILEGES IN SCHEMA construction GRANT ALL ON TABLES TO anon, authenticated, service_role;
    ALTER DEFAULT PRIVILEGES IN SCHEMA construction GRANT ALL ON SEQUENCES TO anon, authenticated, service_role;
    ALTER DEFAULT PRIVILEGES IN SCHEMA construction GRANT ALL ON ROUTINES TO anon, authenticated, service_role;
    ```

    **Migration 001 — create_profiles.sql:**
    - `construction.profiles` table (id UUID PK references auth.users, email, full_name, avatar_url, phone, is_active, created_at, updated_at)
    - `construction.handle_new_user()` trigger function (SECURITY DEFINER, SET search_path = construction)
    - Trigger `on_auth_user_created` on `auth.users` AFTER INSERT

    **Migration 002 — create_companies.sql:**
    - `construction.companies` table with all fields from MRD.md (id, name, slug UNIQUE, owner_id, address, contact_phone, contact_email, tin, plan CHECK, plan_status CHECK, trial_ends_at, current_period_start, current_period_end, max_projects, max_employees, max_users, created_at, updated_at)
    - `construction.company_members` table with partial unique index `idx_one_owner_per_company` (WHERE role = 'owner')
    - UNIQUE(user_id) on company_members (one company per user for v1)
    - `construction.company_invitations` table with partial unique index `idx_unique_pending_invitation` (WHERE status = 'pending')
    - All indexes from MRD.md: idx_company_members_company, idx_company_members_user, idx_invitations_token, idx_invitations_email

    CRITICAL: Also add the composite index for RLS performance (from PITFALLS.md Pitfall 2):
    ```sql
    CREATE INDEX idx_company_members_user_company ON construction.company_members(user_id, company_id);
    ```

    **Migrations 003-014:** Copy exact table DDL from MRD.md for projects, materials/project_inventory, stock_reservations/inventory_transfers, inventory_logs, suppliers/purchase_orders/po_items/deliveries/delivery_items, employees/attendance, cash_advances/employee_loans, payroll/payroll_items, project_costs/project_cost_summary view, expenses, equipment/equipment_assignments, documents, notifications/audit_logs.

    **Migration 015 — create_subscriptions.sql:**
    - `construction.subscriptions` table from MRD.md

    **Migration 016 — create_rls_policies.sql:**
    Copy the ENTIRE RLS section from MRD.md including:
    - ALTER TABLE ... ENABLE ROW LEVEL SECURITY for ALL tables (companies, profiles, company_members, company_invitations, projects, materials, project_inventory, stock_reservations, inventory_transfers, inventory_logs, suppliers, purchase_orders, po_items, deliveries, delivery_items, employees, attendance, cash_advances, employee_loans, payroll, payroll_items, project_costs, expenses, equipment, equipment_assignments, documents, notifications, audit_logs, subscriptions)
    - Helper functions: `auth.user_company_id()`, `auth.user_role()`, `auth.is_company_admin()` — all SECURITY DEFINER STABLE
    - All named policies from MRD.md
    - For tables not explicitly listed in MRD.md RLS section, apply the standard company isolation pattern:
      ```sql
      CREATE POLICY "Company isolation" ON construction.<table_name> FOR ALL
        USING (company_id = auth.user_company_id());
      ```
    - For profiles: add an INSERT policy allowing auth trigger:
      ```sql
      CREATE POLICY "Service role can insert profiles" ON construction.profiles
        FOR INSERT WITH CHECK (true);
      ```
    - For company_members: add INSERT policy for the company creation flow (service-role or new user creating their own membership)

    **Migration 016 also includes the updated_at trigger (FOUND-04):**
    ```sql
    CREATE OR REPLACE FUNCTION construction.update_updated_at()
    RETURNS TRIGGER AS $$
    BEGIN
      NEW.updated_at = now();
      RETURN NEW;
    END;
    $$ LANGUAGE plpgsql;
    ```
    Apply trigger to: companies, profiles, projects, materials, employees, suppliers, purchase_orders, payroll, equipment.

    **Migration 017 — create_transaction_functions.sql:**
    Copy ALL 6 functions VERBATIM from MRD.md:
    - `construction.fn_record_delivery(p_purchase_order_id UUID, p_received_by UUID, p_notes TEXT, p_items JSONB)` RETURNS UUID
    - `construction.fn_reserve_stock(p_project_id UUID, p_material_id UUID, p_quantity NUMERIC, p_purpose TEXT, p_reserved_by UUID)` RETURNS UUID
    - `construction.fn_fulfill_reservation(p_reservation_id UUID, p_performed_by UUID)` RETURNS VOID
    - `construction.fn_complete_transfer(p_transfer_id UUID, p_approved_by UUID)` RETURNS VOID
    - `construction.fn_report_waste(p_project_id UUID, p_material_id UUID, p_quantity NUMERIC, p_waste_reason TEXT, p_notes TEXT, p_performed_by UUID)` RETURNS UUID
    - `construction.fn_cancel_reservation(p_reservation_id UUID, p_performed_by UUID)` RETURNS VOID

    All functions must be SECURITY DEFINER with SET search_path = construction.

    Also include the PO number auto-generation trigger function and trigger from MRD.md:
    - `construction.generate_po_number()` function
    - Trigger `set_po_number` BEFORE INSERT ON purchase_orders

    **After creating all files**, start Supabase local and run migrations:
    ```bash
    npx supabase start
    npx supabase db reset
    ```
  </action>
  <verify>
    <automated>cd /Users/berltreasurecampomanes/Documents/Github\ Builds/construction && npx supabase db reset 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/ contains exactly 18 files (000 through 017)
    - 20260101000000_create_schema.sql contains "CREATE SCHEMA IF NOT EXISTS construction"
    - 20260101000001_create_profiles.sql contains "CREATE TABLE construction.profiles"
    - 20260101000001_create_profiles.sql contains "handle_new_user"
    - 20260101000002_create_companies.sql contains "CREATE TABLE construction.companies"
    - 20260101000002_create_companies.sql contains "CREATE TABLE construction.company_members"
    - 20260101000002_create_companies.sql contains "CREATE TABLE construction.company_invitations"
    - 20260101000002_create_companies.sql contains "idx_one_owner_per_company"
    - 20260101000002_create_companies.sql contains "idx_company_members_user_company"
    - 20260101000016_create_rls_policies.sql contains "ENABLE ROW LEVEL SECURITY"
    - 20260101000016_create_rls_policies.sql contains "auth.user_company_id()"
    - 20260101000016_create_rls_policies.sql contains "auth.user_role()"
    - 20260101000016_create_rls_policies.sql contains "auth.is_company_admin()"
    - 20260101000016_create_rls_policies.sql contains "update_updated_at"
    - 20260101000017_create_transaction_functions.sql contains "fn_record_delivery"
    - 20260101000017_create_transaction_functions.sql contains "fn_reserve_stock"
    - 20260101000017_create_transaction_functions.sql contains "fn_fulfill_reservation"
    - 20260101000017_create_transaction_functions.sql contains "fn_complete_transfer"
    - 20260101000017_create_transaction_functions.sql contains "fn_report_waste"
    - 20260101000017_create_transaction_functions.sql contains "fn_cancel_reservation"
    - 20260101000017_create_transaction_functions.sql contains "generate_po_number"
    - npx supabase db reset completes without errors
  </acceptance_criteria>
  <done>All 18 Supabase migration files exist with the complete construction schema. `npx supabase db reset` runs successfully, creating all tables, indexes, RLS policies, triggers, helper functions, and all 6 transaction functions in the local Supabase instance.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx supabase db reset` applies all 18 migrations without errors
3. All migration files match MRD.md schema definitions
4. RLS enabled on every construction schema table
5. All 6 transaction functions callable via `supabase.rpc()`
</verification>

<success_criteria>
- Next.js project boots on localhost:3000
- Supabase local instance running with construction schema
- All 25+ tables exist with correct columns, constraints, and indexes
- RLS policies enforce company_members-based isolation on all tables
- 6 transaction functions deployed (fn_record_delivery, fn_reserve_stock, fn_fulfill_reservation, fn_complete_transfer, fn_report_waste, fn_cancel_reservation)
- updated_at triggers applied to 9 tables
- Composite index on company_members(user_id, company_id) for RLS performance
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
